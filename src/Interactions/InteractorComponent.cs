using Bang.Components;

namespace Bang.Interactions
{
    /// <summary>
    /// This component is to signalize that an entity is able to interact with other objects.
    /// </summary>
    public struct InteractorComponent : IComponent { }
}
